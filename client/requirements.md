## Packages
framer-motion | Complex animations and scroll reveals
react-hook-form | Form handling for the waitlist
@hookform/resolvers | Zod resolver for react-hook-form
zod | Schema validation

## Notes
The backend provides a single API endpoint `POST /api/leads` for the waitlist form.
The design focuses on a "Stealth Mode" aesthetic with premium corporate blue tones.
